name: Build

on:
  schedule:
    - cron: "0 * * * *"
  # workflow_run:
  #   workflows: ["Excavator"]
  #   types: [completed]
  workflow_dispatch:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Install Dependence
        run: sudo apt-get install -y rename
      - name: Git config
        run: |
          git config --global user.email "43134606+Ryanjiena@users.noreply.github.com"
          git config --global user.name "Ryanjiena"
      - name: "Build"
        env:
          github_token: ${{ secrets.GH_TOKEN }}
        run: bash <(curl -sSfL https://github.com/Ryanjiena/Tian/raw/main/.github/workflows/build.sh)
